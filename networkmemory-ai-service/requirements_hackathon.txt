# ================================================
# HACKATHON-OPTIMIZED REQUIREMENTS
# Installs in 30 seconds, ZERO compilation errors
# ================================================

# Web Framework - FastAPI for async and automatic API docs
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
httpx==0.25.1

# AI/ML APIs - Free tier available for all
google-generativeai==0.3.1  # Gemini - 1500 requests/day free
assemblyai==0.20.0          # AssemblyAI - 5 hours/month free (does transcription + diarization!)
elevenlabs==0.2.26          # ElevenLabs - 10k chars/month free
openai==1.3.5               # OpenAI - for embeddings

# Audio Processing - Lightweight only (no compilation needed)
pydub==0.25.1              # Audio manipulation (uses ffmpeg)

# Utilities
python-dotenv==1.0.0       # Environment variables

# Data processing - NumPy and Pandas (have wheels for Python 3.11)
numpy>=1.26.0,<1.27.0      # Numerical computing
pandas>=2.1.0,<2.2.0       # Data manipulation

# Network & Graph (for Day 2)
networkx==3.2.1            # Graph algorithms

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Database (optional - Python is stateless, but useful for testing)
psycopg2-binary==2.9.9     # PostgreSQL adapter
asyncpg==0.29.0            # Async PostgreSQL driver
sqlalchemy[asyncio]==2.0.23  # ORM with async support

# Agent Framework (for Day 2)
crewai==0.11.0

# ================================================
# REMOVED (compilation issues on Windows):
# ================================================
# faster-whisper==0.9.0     # Needs av (PyAV) which won't compile
# noisereduce==3.0.0        # Needs librosa compilation
# librosa==0.10.1           # Complex C dependencies
# soundfile==0.12.1         # Native library issues
#
# SOLUTION: Use AssemblyAI for transcription + diarization
# One API call instead of local Whisper - simpler and works!
